# ===================================================================================
#  The SocialOctoMap CMake configuration file
#
#             ** File generated automatically, do not modify **
#
#  Usage from an external project:
#    In your CMakeLists.txt, add these lines:
#
#    FIND_PACKAGE(SOCIAL_OCTOMAP REQUIRED )
#    INCLUDE_DIRECTORIES(${SOCIAL_OCTOMAP_INCLUDE_DIRS})
#    TARGET_LINK_LIBRARIES(MY_TARGET_NAME ${SOCIAL_OCTOMAP_LIBRARIES})
#
#
#    This file will define the following variables:
#      - SOCIAL_OCTOMAP_LIBRARIES      : The list of libraries to links against.
#      - SOCIAL_OCTOMAP_LIBRARY_DIRS   : The directory where lib files are. Calling
#                                 LINK_DIRECTORIES with this path is NOT needed.
#      - SOCIAL_OCTOMAP_INCLUDE_DIRS   : The SocialOctoMap include directories.
#      - SOCIAL_OCTOMAP_MAJOR_VERSION  : Major version.
#      - SOCIAL_OCTOMAP_MINOR_VERSION  : Minor version.
#      - SOCIAL_OCTOMAP_PATCH_VERSION  : Patch version.
#      - SOCIAL_OCTOMAP_VERSION        : Major.Minor.Patch version.
#
# ===================================================================================

@PACKAGE_INIT@

set(SOCIAL_OCTOMAP_MAJOR_VERSION "@SOCIAL_OCTOMAP_MAJOR_VERSION@")
set(SOCIAL_OCTOMAP_MINOR_VERSION "@SOCIAL_OCTOMAP_MINOR_VERSION@")
set(SOCIAL_OCTOMAP_PATCH_VERSION "@SOCIAL_OCTOMAP_PATCH_VERSION@")
set(SOCIAL_OCTOMAP_VERSION "@SOCIAL_OCTOMAP_VERSION@")

set_and_check(SOCIAL_OCTOMAP_INCLUDE_DIRS "@PACKAGE_SOCIAL_OCTOMAP_INCLUDE_DIRS@")
set_and_check(SOCIAL_OCTOMAP_LIBRARY_DIRS "@PACKAGE_SOCIAL_OCTOMAP_LIB_DIR@")

# Set library names
set(SOCIAL_OCTOMAP_LIBRARIES
  "@PACKAGE_SOCIAL_OCTOMAP_LIB_DIR@/@SOCIAL_OCTOMAP_LIBRARY@"
  "@PACKAGE_SOCIAL_OCTOMAP_LIB_DIR@/@OCTOMATH_LIBRARY@"
)

# Additionally set the variables using a lower-case project name.
# This fixes discovery for downstream packages that search for lower-case social_octomap, which has been common in downstream code:
set(social_octomap_MAJOR_VERSION "${SOCIAL_OCTOMAP_MAJOR_VERSION}")
set(social_octomap_MINOR_VERSION "${SOCIAL_OCTOMAP_MINOR_VERSION}")
set(social_octomap_PATCH_VERSION "${SOCIAL_OCTOMAP_PATCH_VERSION}")
set(social_octomap_VERSION "${SOCIAL_OCTOMAP_VERSION}")
set(social_octomap_INCLUDE_DIRS "${SOCIAL_OCTOMAP_INCLUDE_DIRS}")
set(social_octomap_LIBRARY_DIRS "${SOCIAL_OCTOMAP_LIBRARY_DIRS}")
set(social_octomap_LIBRARIES "${SOCIAL_OCTOMAP_LIBRARIES}")


@SOCIAL_OCTOMAP_INCLUDE_TARGETS@
